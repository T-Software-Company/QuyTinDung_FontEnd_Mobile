import {Image} from 'react-native';
import {AppIcons} from '../icons';

const IconComponent = () => {
  return <Image source={AppIcons.home} style={{width: 24, height: 24}} />;
};

export default IconComponent;
